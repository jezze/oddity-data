CREATE TABLE apps (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    short TEXT NOT NULL,
    icon TEXT,
    preview TEXT,
    date TEXT,
    author TEXT,
    portauthor TEXT,
    homepage TEXT,
    description TEXT
);
